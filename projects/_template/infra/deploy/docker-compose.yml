version: "3.9"
services:
  app:
    image: replace-me
    build:
      context: ../../..
      dockerfile: ./infra/deploy/Dockerfile
    env_file:
      - ./.env.example
    ports:
      - "3000:3000"
    restart: unless-stopped

# Placeholder deployment compose.
# Replace service definitions with generated project containers.
